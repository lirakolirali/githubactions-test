name: CI Training

on:
  push:
  pull_request:

jobs:
  hello-ci:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Say hello
        run: echo "Hello Github Actions"